/**
 * * Base dot configuration
 */
export type DotsConfig = {
  slides: any[];
  activeIndex: number;
};


/**
 * * Current dot status
 */
export type DotStatus = {
  index: number;
  active: boolean;
};